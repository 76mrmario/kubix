apiVersion: v1
kind: Service
metadata:
  name: kubix-service
spec:
  selector:
    app: kubix
  ports:
    - name: http
      port: 80
      # containers port
      targetPort: 8080
  type: ClusterIP
